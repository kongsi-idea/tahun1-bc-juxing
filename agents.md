# tahun1-bc-juxing（句型跳跳队）

一年级华文 DSKP 5.4.1「认识陈述、疑问、祈使、感叹等句子功能」摄像头体感游戏。学生分组（建议 4-8 人）站在镜头前，看题目跳左边或右边选句型，全组一致答对才得分。

这是 `teaching-tools/` 目录**第一个用摄像头做姿态侦测的工具**，没有先例可循，以下几条是这类工具的先例规范，之后同类工具应参考：

## 隐私（硬性规定，不是可选项）

- 摄像头画面**只在学生浏览器本机做姿态推理，视频/影像画面绝对不上传到任何服务器**。整个流程从 `getUserMedia` 拿到的 video stream 只喂给本机的 MediaPipe 推理，从来没有任何 `fetch`/上传动作把画面传出这台电脑。
- 推理结果只保留「侦测到几个人、大概站左边还是右边」这种聚合数字，不做人脸辨识、不记录任何可识别学生身份的资料。

## 技术选型

- 姿态侦测：`@mediapipe/tasks-vision` 的 `PoseLandmarker`，用官方内建的 `numPoses` 参数支持多人侦测（本工具设为 8）。
- 模型用 **Lite 版**（`pose_landmarker_lite.task`，约 5.8MB），wasm 运行时和模型档案都打包在 `public/mediapipe/` 与 `public/models/` 一起部署，**不依赖课堂当场去 CDN 现抓**，避免上课当天因学校 wifi 不稳卡在加载画面。
- 分区判定：用左右髋部关节（landmark 23/24）中点估计站位，中线保留 8% 宽度的缓冲死区，太靠近中线的人不计入任何一侧。

## 分组人数

- 建议 **4-8 人一组**上场，不做全班同时进行——姿态侦测在人多密集/遮挡时误判率会明显上升，缩小到 4-8 人可控范围更稳。这条也要写进 Hub 上架时的 `prep` 字段，让老师看得到。

## 内容来源

- 题库（`src/data/questions.js`）已核对课本页码来源，`source.book === true` 的题目是直接引用《一年级华文课本》原句；`false` 的是自编例句，用法已核对符合马来西亚华语标准。新增题目时要保持这个标注习惯，不能编造未查证的课本对照。

## 已知限制 / v1 不做

- 没有强制摄像头校准确认步骤（先上线看效果，实测发现误判频繁再加）。
- 排行榜是 `localStorage`，本机浏览器专属，不是跨设备共享。
